<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <ul><!-- url in the loop is a generic name -->
  <% for(var url in urls ) { %>
    <li><%= `${url}: ${urls[url]}` %></li>
  <% } %>
  </ul>
</body>
</html>
