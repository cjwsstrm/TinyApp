<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  <body style="font-size:20px; padding:50px;">
    <% include ../views/partials/_header.ejs%>
    <p>
      <%= `Current Address: ${urls[shortURL].longurl}`%>
    </p>
    <p>
      <%= `ShortURL: ${shortURL}`%>
    </p>
    <p>
    <form method="POST" action="/urls/<%= shortURL %>">
      <label for="URL">Update URL</label>
      <input type="text" name="URL">
      <button type="submit">Update</button>
    </form>
    </p>
  </body>
</html>
